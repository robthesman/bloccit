<h1>Edit SponsoredPost</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for sponsored posts:</p>
     <ul>
       <li>Make sure the posts are appropriate.</li>
       <li>Never insult dogs.</li>
       <li>Smile when you type.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @sponsoredpost do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :name, class: 'form-control', placeholder: "Enter sponsored post name" %>
       </div>
       
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter sponsored post description" %>
       </div>
       
        <div class="form-group">
         <%= f.label :price %>
         <%= f.text_area :price, rows: 8, class: 'form-control', placeholder: "Enter price" %>
       </div>
       
       <div class="form-group">
 <!-- #11 -->
         <%= f.label :public, class: 'checkbox' do %>
           <%= f.check_box :public %> Public topic
         <% end %>
       </div>
       <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>